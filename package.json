{
  "name": "hoth-poc",
  "private": true,
  "version": "0.0.1",
  "description": "Hoth Trip-Planner POC — proves hard-coded vs dynamic-bundle skill delivery yield identical agent behavior on Flue + Cloudflare Sandbox",
  "type": "module",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "smoke": "node scripts/node-smoke.mjs",
    "test": "node scripts/admin.test.mjs",
    "dev:b": "pnpm --filter backend-b dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build:b": "pnpm --filter backend-b build",
    "deploy:b": "pnpm --filter backend-b deploy",
    "deploy:frontend": "pnpm --filter frontend deploy",
    "deploy:agent": "node scripts/deploy-agent.mjs",
    "deploy:agents": "pnpm bundle && node scripts/deploy-agent.mjs --all && pnpm deploy:frontend",
    "deploy": "pnpm bundle && pnpm deploy:b && node scripts/deploy-agent.mjs --all && pnpm deploy:frontend",
    "mint-token": "node scripts/mint-token.mjs",
    "acceptance": "node scripts/acceptance.mjs",
    "sessions": "node scripts/session-costs.mjs"
  }
}
